/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['stablo-template.vercel.app', 'cdn.sanity.io','www.w3schools.com', 'plus.unsplash.com','cdn.pixabay.com']
    }
}

module.exports = nextConfig
